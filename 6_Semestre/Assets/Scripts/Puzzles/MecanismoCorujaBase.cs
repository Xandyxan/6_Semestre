using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MecanismoCorujaBase : MonoBehaviour, IInteractable
{
    public void Interact()
    {
       // jogador pode selecionar entre uma list de IInteractable
       // Renderer ren = interativos[i].GetComponent<Renderer>();
       //if(rend) rend.EnableKeyword("EMISSION");
    }
}
